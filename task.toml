artifacts = ["/app/report.json"]

[task]
name = "dynamo/log-report"
description = "Extract data and save as JSON."

[metadata]
category = "coding"             
subcategory = "data_extraction"          
task_objective = ["write_code"]     
artifact_type = ["json"]      
expert_time_estimate_hours = 0.3
model_tested = "GPT-4"
agent_tested = "Terminus-2"
difficulty_explanation = "Simple JSON extraction."
solution_explanation = "Read the log and output JSON."
verification_explanation = "Check if the JSON contains the right keys."

[verifier]
timeout_sec = 120.0

[agent]
timeout_sec = 120.0

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 10240
gpus = 0
allow_internet = true
mcp_servers = []